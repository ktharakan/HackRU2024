import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ['canvas.instructure.com',"rutgers.instructure.com"], // Replace with your image host(s)
  },
};

export default nextConfig;
